{
    "private": true,
    "scripts": {
        "build": "turbo build",
        "dev": "npm run db && npm run m:run && turbo dev",
        "lint": "turbo lint",
        "test": "turbo test",
        "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
        "db": "./scripts/docker_db",
        "m:create": "npm run db && npx prisma migrate dev",
        "m:run": "npm run db && npx prisma migrate deploy",
        "m:seed": "npm run db && npx prisma db seed",
        "generate-prisma-client": "npx prisma migrate dev",
        "codegen": "npm run generate-prisma-client"
    },
    "devDependencies": {
        "@types/node": "^22.7.5",
        "eslint": "^8.57.0",
        "prettier": "^3.2.5",
        "ts-node": "^10.9.2",
        "turbo": "^2.1.3",
        "typescript": "^5.6.3"
    },
    "name": "with-vite",
    "packageManager": "npm@10.8.2",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "dependencies": {
        "@prisma/client": "^5.20.0"
    },
    "prisma": {
        "seed": "ts-node prisma/seeds/index.ts"
    }
}
